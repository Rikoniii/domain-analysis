# Инструкции по доступу

## 📖 Как мы это сделали и зачем?

Мы реализовали простую систему доступа с базовой аутентификацией для админ-панели и тестовым аккаунтом для демонстрации функционала личного кабинета.

**Почему именно так?** 
- Для админ-панели используется базовая HTTP-аутентификация - простая и достаточная для разработки.
- Тестовый аккаунт позволяет демонстрировать функционал без настройки реальной SMS-авторизации.
- В продакшене нужно добавить более надежную систему аутентификации.

## Доступ к админ-панели

### URL
Откройте в браузере: `http://localhost:8000/frontend/admin.html`

### Данные для входа
- **Логин:** `admin`
- **Пароль:** `admin123`

### Примечание
При первом открытии админ-панели появится запрос на ввод логина и пароля. После успешного входа доступ сохраняется в браузере.

**Как это работает?** Браузер использует встроенную HTTP Basic Authentication. Данные сохраняются в браузере до закрытия вкладки или очистки кэша.

---

## Тестовый аккаунт пользователя (Анна Петрова Васильевна)

### Для входа в личный кабинет:
1. Откройте страницу "Личный кабинет" (`http://localhost:8000/frontend/profile.html`).
2. Введите номер телефона: **+7 (495) 123-45-67**.
3. Выберите способ входа (SMS-код или звонок).
4. Нажмите "Войти".

### Информация о тестовом аккаунте:
- **Имя:** Анна Петрова Васильевна
- **Телефон:** +7 (495) 123-45-67
- **Email:** anna@example.com
- **Пожертвований:** 5
- **Всего пожертвовано:** 2 500 ₽

### Примечание
Этот аккаунт используется для демонстрации функционала личного кабинета. При вводе этого номера телефона вход происходит автоматически без отправки SMS.

**Как это сделано?** В коде API есть проверка: если номер телефона совпадает с тестовым, код подтверждения возвращается сразу без реальной отправки SMS. Это позволяет тестировать функционал без настройки SMS-сервиса.

---

## Запуск сервера

1. Запустите `start-server.bat` (Windows)
2. Или вручную: `python -m http.server 8000`
3. Откройте в браузере: `http://localhost:8000/frontend/`

