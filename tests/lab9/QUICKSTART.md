# Лабораторная работа №9: Генерация тестовых данных

---

## 🚀 Быстрый старт

### Установка

```bash
cd backend
pip install -r requirements.txt
```

### Запуск

**Базовый запуск (рекомендуется для первого раза):**
```bash
python backend/generate_test_data.py
```

Создаст:
- 50 пользователей
- 200 пожертвований
- 30 подписок
- 40 способов оплаты

**Настройка количества данных:**
```bash
python backend/generate_test_data.py 100 500 50 80
```

Параметры: `<пользователи> <пожертвования> <подписки> <способы_оплаты>`

### Проверка результатов

После генерации скрипт автоматически:
1. ✅ Валидирует данные
2. ✅ Проверяет целостность БД
3. ✅ Выводит статистику

---

## 📋 Описание

В данной лабораторной работе реализована система генерации тестовых данных для проекта приюта "Дом Лап" с использованием библиотеки **Faker** для Python. Генератор создает реалистичные тестовые данные для всех основных таблиц базы данных, обеспечивая целостность данных и их валидность.

## 🛠 Используемые инструменты

### Основной инструмент: **Faker**

**Faker** — это библиотека Python для генерации фейковых данных. Она позволяет создавать реалистичные данные различных типов:
- Имена, адреса, телефоны
- Email адреса
- Даты и время
- UUID и другие идентификаторы
- И многое другое

**Версия:** Faker 24.0.0

**Локализация:** Используется русская локаль (`ru_RU`) для генерации русскоязычных имен и данных.

### Альтернативные инструменты (рассмотренные, но не использованные)

1. **Mockaroo** — веб-сервис для генерации CSV/JSON/SQL данных
2. **factory_boy** — генерация объектов для ORM (Django, SQLAlchemy)
3. **Hypothesis** — property-based генерация данных для тестирования

**Выбор Faker обоснован:**
- Простота использования
- Хорошая интеграция с Python и SQLAlchemy
- Поддержка различных локалей
- Активная разработка и поддержка
- Не требует внешних зависимостей (веб-сервисов)

## 📁 Структура проекта

```
backend/
├── generate_test_data.py    # Основной скрипт генерации данных
├── database.py              # Модели базы данных
└── requirements.txt         # Зависимости (включая Faker)

tests/
└── lab9/
    └── QUICKSTART.md        # Этот файл
```

## 📊 Генерируемые данные

### 1. Пользователи (Users)

**Таблица:** `users`

**Генерируемые поля:**
- `phone` — уникальный номер телефона (нормализованный формат +7XXXXXXXXXX)
- `email` — email адрес (90% пользователей имеют email)
- `full_name` — полное имя (русские имена)
- `created_at` — дата создания (случайная дата за последний год)

**Особенности:**
- Гарантируется уникальность телефонов
- Автоматическая нормализация номеров телефонов
- Проверка на дубликаты перед вставкой

### 2. Способы оплаты (PaymentMethods)

**Таблица:** `payment_methods`

**Генерируемые поля:**
- `user_id` — связь с пользователем
- `provider` — провайдер оплаты (yoomoney, stripe, paypal)
- `provider_payment_token` — токен платежного метода (70% имеют токен)
- `last4` — последние 4 цифры карты (80% имеют)
- `is_active` — активность способа оплаты (75% активны)
- `created_at` — дата создания

**Особенности:**
- Связь с существующими пользователями
- Реалистичное распределение активных/неактивных методов

### 3. Пожертвования (Donations)

**Таблица:** `donations`

**Генерируемые поля:**
- `user_id` — связь с пользователем (80% связаны с пользователем, 20% анонимные)
- `public_name` — публичное имя или "Анонимно"
- `phone` — телефон (если не анонимно)
- `email` — email (если не анонимно)
- `amount` — сумма пожертвования (от 100 до 50,000 рублей)
- `purpose` — назначение (food, medical, maintenance, general)
- `is_recurring` — регулярное пожертвование (если связано с подпиской)
- `subscription_id` — связь с подпиской (30% связаны с подписками)
- `provider` — провайдер оплаты
- `provider_payment_id` — ID платежа (UUID)
- `status` — статус (pending, succeeded, canceled, failed)
- `paid_at` — дата оплаты (если статус succeeded)
- `created_at` — дата создания (за последние 6 месяцев)

**Особенности:**
- Реалистичное распределение анонимных/именных пожертвований
- Связь с подписками для регулярных пожертвований
- Валидация минимальной суммы (100 рублей)

### 4. Подписки (Subscriptions)

**Таблица:** `subscriptions`

**Генерируемые поля:**
- `user_id` — связь с пользователем
- `payment_method_id` — способ оплаты
- `amount` — сумма подписки (от 200 до 10,000 рублей)
- `purpose` — назначение
- `frequency` — частота списания (weekly, monthly, quarterly)
- `status` — статус (active, canceled, paused)
- `next_charge_at` — дата следующего списания (для активных)
- `last_charge_at` — дата последнего списания (50% имеют)
- `created_at` — дата создания (за последний год)
- `canceled_at` — дата отмены (для отмененных)

**Особенности:**
- Автоматическое создание пожертвований для активных подписок
- Корректный расчет дат следующего списания
- Реалистичное распределение статусов

## ✅ Валидация данных

После генерации данных автоматически выполняется валидация:

### Проверки целостности:

1. **Уникальность телефонов** — проверка на дубликаты номеров телефонов
2. **Минимальная сумма пожертвований** — проверка, что все суммы >= 100 рублей
3. **Связи между таблицами:**
   - Пожертвования должны ссылаться на существующих пользователей
   - Подписки должны ссылаться на существующих пользователей
   - Подписки должны ссылаться на существующие способы оплаты
   - Способы оплаты должны ссылаться на существующих пользователей

### Результаты валидации:

- ✅ **Успех** — все проверки пройдены, данные готовы к использованию
- ⚠️ **Предупреждения** — не критичные проблемы (например, пустые таблицы)
- ❌ **Ошибки** — критические проблемы, требующие исправления

## 📈 Статистика генерации

После успешной генерации выводится статистика:

- Общее количество созданных записей по каждой таблице
- Количество активных подписок
- Количество успешных пожертвований
- Общая сумма пожертвований

## 💻 Пример вывода

```
============================================================
  Генератор тестовых данных для приюта 'Дом Лап'
============================================================

[INFO] Генерация пользователей: 50
  Создано пользователей: 10/50
  Создано пользователей: 20/50
  ...
  Создано пользователей: 50/50

[INFO] Генерация способов оплаты: 40
  Создано способов оплаты: 10/40
  ...

[INFO] Генерация подписок: 30
  ...

[INFO] Генерация пожертвований: 200
  Создано пожертвований: 50/200
  ...

[INFO] Валидация данных...
[OK] Валидация пройдена успешно!

============================================================
  Статистика сгенерированных данных:
============================================================
  Пользователей: 50
  Способов оплаты: 40
  Подписок: 30
  Пожертвований: 200

  Активных подписок: 18
  Успешных пожертвований: 145
  Общая сумма пожертвований: 2,345,678.90 ₽

============================================================
  Генерация данных завершена успешно!
============================================================
```

## 🔍 Проверка целостности БД

После генерации данных рекомендуется проверить базу данных:

```bash
python backend/check_db.py
```

Или использовать SQLite клиент:

```bash
sqlite3 backend/shelter.db
```

**Примеры запросов:**

```sql
-- Количество записей в каждой таблице
SELECT 'users' as table_name, COUNT(*) as count FROM users
UNION ALL
SELECT 'donations', COUNT(*) FROM donations
UNION ALL
SELECT 'subscriptions', COUNT(*) FROM subscriptions
UNION ALL
SELECT 'payment_methods', COUNT(*) FROM payment_methods;

-- Проверка связей
SELECT COUNT(*) as orphan_donations
FROM donations d
LEFT JOIN users u ON d.user_id = u.id
WHERE d.user_id IS NOT NULL AND u.id IS NULL;
```

## 🔧 Особенности реализации

### Нормализация телефонов

Все номера телефонов нормализуются к формату `+7XXXXXXXXXX` для обеспечения уникальности и корректного хранения.

### Управление связями

Генератор автоматически создает связи между таблицами:
- Пожертвования связаны с пользователями
- Подписки связаны с пользователями и способами оплаты
- Регулярные пожертвования связаны с подписками

### Реалистичность данных

- Используются русские имена и фамилии
- Телефоны в российском формате
- Email адреса в корректном формате
- Даты распределены реалистично (за последние месяцы/годы)
- Статусы распределены естественным образом

## ⚠️ Обработка ошибок

Генератор обрабатывает следующие ошибки:

1. **IntegrityError** — при попытке создать дубликат (например, одинаковый телефон)
   - Автоматическая генерация нового уникального значения
   - Повторная попытка вставки

2. **Ошибки валидации** — при проверке целостности данных
   - Вывод списка всех найденных ошибок
   - Прерывание выполнения с кодом ошибки

3. **Общие исключения** — любые другие ошибки
   - Откат транзакции (rollback)
   - Вывод трассировки стека для отладки

## 📝 Выполнение работы

### Цель работы

Создать систему генерации тестовых данных для проекта приюта "Дом Лап" с использованием инструментов для автоматической генерации данных. Обеспечить валидность и целостность генерируемых данных, а также проверить корректность работы базы данных с большими объемами тестовых данных.

### Задачи

1. Выбрать и установить инструмент для генерации тестовых данных
2. Создать скрипт генерации данных для всех основных таблиц БД
3. Реализовать валидацию генерируемых данных
4. Проверить целостность базы данных после генерации
5. Документировать процесс генерации данных

### Реализованные функции

#### Генерация пользователей (`generate_user`)
- Генерирует уникальные номера телефонов в формате +7XXXXXXXXXX
- Создает русские имена и фамилии
- Генерирует email адреса (90% пользователей имеют email)
- Устанавливает случайные даты создания за последний год

#### Генерация способов оплаты (`generate_payment_method`)
- Связывает с существующими пользователями
- Генерирует токены платежных методов (70% имеют токен)
- Создает последние 4 цифры карты (80% имеют)
- Распределяет активные/неактивные методы (75% активны)

#### Генерация пожертвований (`generate_donation`)
- Создает пожертвования от 100 до 50,000 рублей
- 80% связаны с пользователями, 20% анонимные
- Распределяет статусы: pending, succeeded, canceled, failed
- 30% связаны с подписками (регулярные пожертвования)
- Устанавливает даты за последние 6 месяцев

#### Генерация подписок (`generate_subscription`)
- Создает подписки от 200 до 10,000 рублей
- Частоты: weekly, monthly, quarterly
- Статусы: active, canceled, paused
- Автоматически создает пожертвования для активных подписок
- Корректно рассчитывает даты следующего списания

### Результаты

#### Созданные файлы:

1. **`backend/generate_test_data.py`** — основной скрипт генерации данных
2. **`tests/lab9/QUICKSTART.md`** — этот файл с полной документацией

#### Обновленные файлы:

1. **`backend/requirements.txt`** — добавлен Faker==24.0.0

### Проверка целостности БД

После генерации данных выполнена проверка:

1. ✅ Все пользователи имеют уникальные телефоны
2. ✅ Все пожертвования имеют сумму >= 100 рублей
3. ✅ Все связи между таблицами корректны
4. ✅ Нет "осиротевших" записей (orphan records)

## 💡 Рекомендации по использованию

1. **Перед генерацией** — убедитесь, что база данных инициализирована
2. **После генерации** — проверьте валидацию и статистику
3. **Для тестирования** — используйте небольшие объемы данных (50-100 записей)
4. **Для демонстрации** — используйте большие объемы (500+ записей)
5. **Перед продакшеном** — всегда проверяйте целостность данных

## 🎯 Выводы

1. **Успешно реализована система генерации тестовых данных** с использованием библиотеки Faker
2. **Создан универсальный скрипт**, который может генерировать различные объемы данных
3. **Реализована валидация данных**, обеспечивающая целостность БД
4. **Данные генерируются реалистично** благодаря использованию русской локали Faker
5. **Система готова к использованию** для тестирования и разработки

## 🛠 Используемые технологии

- **Python 3.x** — язык программирования
- **Faker 24.0.0** — библиотека генерации данных
- **SQLAlchemy 2.0.23** — ORM для работы с БД
- **SQLite** — база данных

## 📚 Источники

- [Документация Faker](https://faker.readthedocs.io/)
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)
- Документация проекта приюта "Дом Лап"
