# Система платежей и пожертвований

## 📖 Как мы это сделали и зачем?

Мы реализовали систему пожертвований с поддержкой разовых и регулярных платежей. Система работает в двух режимах:
- **Демо-режим** (для разработки) - платежи не списываются реально, но вся логика работает
- **Продакшен** (с YooMoney) - реальные платежи через платежный шлюз

**Почему именно так?** Это позволяет разрабатывать и тестировать функционал без реальных списаний, а затем легко переключиться на реальные платежи.

---

## 🔄 Как работает процесс пожертвования

### Общий процесс

1. **Выбор суммы и способа оплаты**: Пользователь выбирает сумму пожертвования и способ оплаты на странице `donate.html`.

2. **Обработка способа оплаты**:
   - **Банковская карта**: Открывается модальное окно для ввода данных карты
   - **СБП**: Открывается модальное окно с QR-кодом (демо-режим)
   - **Другие способы**: Сразу отправляется запрос на API

**Почему модальное окно?** Это улучшает UX - пользователь не покидает страницу, процесс оплаты происходит в удобном интерфейсе.

### Оплата банковской картой

#### Модальное окно ввода карты

При выборе оплаты картой открывается модальное окно (`cardPaymentModal`), которое:

1. **Показывает сохраненные карты** (если пользователь авторизован):
   - Загружает карты из `localStorage` по ключу `paymentCards_{userPhone}`
   - Отображает список сохраненных карт с маскированными номерами
   - Позволяет выбрать сохраненную карту одним кликом

**Зачем сохранять карты?** Для удобства пользователей - не нужно каждый раз вводить данные карты заново.

2. **Форма ввода новой карты**:
   - Номер карты (с автоформатированием: `1234 5678 9012 3456`)
   - Срок действия (формат: `MM/YY`)
   - CVC код (3 цифры)
   - Имя на карте
   - **Чекбокс "Сохранить карту для будущих платежей"**

**Почему автоформатирование?** Улучшает UX - пользователь видит правильно отформатированный номер карты.

#### Сохранение карты

Если пользователь отметил чекбокс "Сохранить карту":
- Карта сохраняется в `localStorage` по ключу `paymentCards_{userPhone}`
- Данные карты (номер, срок действия, имя) сохраняются локально
- CVC код **НЕ сохраняется** (по соображениям безопасности)

**Почему CVC не сохраняется?** Это требование безопасности - CVC код нужен только для подтверждения платежа, его нельзя хранить.

**Почему localStorage, а не сервер?** В текущей реализации для простоты. В продакшене нужно использовать токенизацию и хранить токены на сервере.

#### Использование сохраненной карты

1. При открытии модального окна оплаты:
   - Если есть сохраненные карты → показывается список
   - Если карт нет → сразу показывается форма ввода

2. При выборе сохраненной карты:
   - Форма автоматически заполняется данными карты
   - CVC нужно ввести заново (не сохраняется)
   - Можно отредактировать данные или использовать "Использовать новую карту"

#### Обработка платежа

После ввода/выбора карты:
1. Если выбрано "Сохранить карту" → карта сохраняется в `localStorage`
2. Данные пожертвования отправляются на API (`/api/donations`)
3. В демо-режиме платеж обрабатывается как успешный (без реального списания)
4. В реальной интеграции данные карты отправлялись бы на платежный шлюз (YooMoney, Stripe и т.п.)

---

## 🧪 Тестовые запросы (демо-режим)

### Как это работает в разработке

В текущей реализации:
- **Нет реальных платежных серверов** - используется демо-режим
- API возвращает мок-ответы (`mock_payment_*`)
- Данные карты **не отправляются** на реальные платежные системы
- Все операции выполняются локально в браузере

**Зачем демо-режим?** Позволяет разрабатывать и тестировать функционал без необходимости настраивать реальные платежи и тратить деньги.

### Что происходит при оплате:

1. Форма пожертвования отправляется на `http://localhost:5000/api/donations`
2. Backend создает запись в БД со статусом `pending`
3. Backend возвращает мок-URL платежа
4. Frontend обрабатывает ответ и показывает сообщение об успехе
5. Пожертвование сохраняется в локальную БД (`donations-db.js`)

**Почему два места хранения?** 
- БД на сервере - для админки и аналитики
- localStorage - для быстрого доступа на фронтенде и офлайн-работы

---

## 🔁 Регулярные пожертвования

Для регулярных пожертвований:
- Требуется авторизация в личном кабинете
- Требуется наличие активной сохраненной карты
- Карта должна быть сохранена в профиле и помечена как активная
- При создании регулярного пожертвования создается подписка в `localStorage`

**Как это работает?** Система создает подписку, которая периодически (еженедельно/ежемесячно) списывает средства с сохраненной карты.

**Почему нужна авторизация?** Чтобы связать подписку с конкретным пользователем и его картой.

---

## 🔒 Безопасность

⚠️ **Важно**: В текущей реализации данные карт хранятся в `localStorage` в открытом виде. 

**Почему это проблема?** Любой скрипт на странице может получить доступ к данным карт. Это небезопасно.

В продакшене необходимо:
- Шифровать данные карт перед сохранением
- Использовать токенизацию (сохранять токены вместо реальных данных)
- Использовать PCI DSS совместимые решения
- Никогда не отправлять CVC код на свой сервер (только на платежный шлюз)

**Почему токенизация?** Платежные системы (YooMoney, Stripe) предоставляют токены вместо реальных данных карты. Токен можно безопасно хранить и использовать для повторных списаний.

---

## ✅ Что уже реализовано

### Шаг 1: База данных ✅

- SQLite БД с таблицами: `users`, `payment_methods`, `donations`, `subscriptions`
- Автоматическая инициализация при первом запуске
- Миграция данных из JSON в БД

**Как это сделано?** Используем SQLAlchemy ORM для работы с БД. При первом запуске API автоматически создает все таблицы.

**Почему SQLite?** Простота развертывания - не нужен отдельный сервер БД. Для продакшена лучше использовать PostgreSQL.

### Шаг 2: API эндпоинты ✅

- `GET /api/admin/donations` - список донатов для админки
- `GET /api/admin/donations/monthly-stats` - статистика для графика
- Админ-панель переключена на использование API (с fallback на локальную БД)

**Почему fallback?** Если API недоступен, админка все равно работает с локальными данными.

### Шаг 3: Создание донатов и YooMoney ✅

- `POST /api/donations` - создание доната и инициирование платежа
- Интеграция с YooMoney API (с мок-режимом для разработки)
- Страница донатов (`donate.html`) отправляет данные на API

**Как работает мок-режим?** Если не настроены ключи YooMoney, API возвращает мок-ответы, имитируя успешный платеж.

### Шаг 4: Webhook обработчик ✅

- `POST /api/yoomoney/webhook` - обработка уведомлений от YooMoney о статусе платежа
- Проверка подписи webhook (требует настройки в продакшене)
- Автоматическое обновление статуса донатов
- Автоматическое создание подписок после успешного первого платежа

**Как это работает?** YooMoney отправляет уведомление на наш сервер, когда платеж завершен. Мы обновляем статус в БД.

**Почему webhook?** Это стандартный способ получения уведомлений от платежных систем - они сами сообщают нам о статусе платежа.

### Шаг 5: Подписки и крон-джоб ✅

- Таблица `subscriptions` в БД
- Функция `process_recurring_charges()` для обработки регулярных списаний
- API эндпоинты для управления подписками

**Как работает крон-джоб?** Периодически (например, каждый час) запускается функция, которая проверяет все активные подписки и списывает средства.

---

## 🔧 Настройка для продакшена

### 1. Получение ключей YooMoney

1. Зарегистрируйтесь в [YooMoney для бизнеса](https://yoomoney.ru/business)
2. Создайте магазин/приложение
3. Получите:
   - **Shop ID** (ID магазина)
   - **Secret Key** (секретный ключ для API)
   - **Webhook Secret** (секрет для проверки подписи webhook)

**Почему нужны эти ключи?** Для аутентификации в API YooMoney и проверки подлинности webhook-уведомлений.

### 2. Настройка переменных окружения

Создайте файл `.env` в папке `backend/`:

```env
YOOMONEY_SHOP_ID=your_shop_id_here
YOOMONEY_SECRET_KEY=your_secret_key_here
YOOMONEY_WEBHOOK_SECRET=your_webhook_secret_here
BASE_URL=https://your-domain.com
```

**Важно:** Не коммитьте `.env` в git (уже добавлен в `.gitignore`)

**Почему переменные окружения?** Безопасность - секретные ключи не должны быть в коде.

### 3. Обновление app.py для чтения .env

Добавьте в начало `app.py`:

```python
from dotenv import load_dotenv
load_dotenv()
```

И установите пакет:
```bash
pip install python-dotenv
```

### 4. Настройка webhook URL в YooMoney

В настройках магазина YooMoney укажите URL для webhook:
```
https://your-domain.com/api/yoomoney/webhook
```

**Для разработки используйте ngrok:**
```bash
ngrok http 5000
```
Затем укажите полученный URL в настройках YooMoney.

**Почему ngrok?** Позволяет пробросить локальный сервер в интернет для тестирования webhook.

### 5. Настройка крон-джоба

#### Windows (Task Scheduler)

1. Откройте Планировщик заданий
2. Создайте новую задачу
3. Триггер: каждые N часов
4. Действие: запустить программу
   - Программа: `python`
   - Аргументы: `-c "from app import process_recurring_charges; process_recurring_charges()"`
   - Рабочая папка: путь к проекту

#### Linux (cron)

Добавьте в crontab:
```bash
0 * * * * cd /path/to/project/backend && python -c "from app import process_recurring_charges; process_recurring_charges()"
```

Это будет запускать обработку каждый час.

**Почему крон-джоб?** Нужен для автоматической обработки регулярных списаний по подпискам.

### 6. Развертывание на сервере

#### Вариант 1: Flask + Gunicorn

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

**Почему Gunicorn?** Это production-ready WSGI сервер для Flask. Встроенный сервер Flask не предназначен для продакшена.

#### Вариант 2: Docker

Создайте `Dockerfile`:
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["gunicorn", "-w", "4", "-b", "0.0.0.0:5000", "app:app"]
```

**Почему Docker?** Упрощает развертывание и обеспечивает одинаковую среду на всех серверах.

### 7. Настройка HTTPS

Webhook от YooMoney требуют HTTPS. Используйте:
- Nginx с Let's Encrypt SSL
- Cloudflare
- Другие решения для HTTPS

**Почему HTTPS обязателен?** Безопасность передачи данных и требование YooMoney для webhook.

---

## 📋 Проверка работы

### Тест создания доната

1. Откройте `http://localhost:8000/frontend/donate.html`
2. Заполните форму
3. Отправьте
4. Проверьте в консоли API, что запрос пришел
5. Проверьте в БД (`shelter.db`), что запись создана

### Тест webhook (в разработке)

Используйте curl для симуляции webhook:

```bash
curl -X POST http://localhost:5000/api/yoomoney/webhook \
  -H "Content-Type: application/json" \
  -H "X-YooMoney-Signature: test_signature" \
  -d '{
    "event": "payment.succeeded",
    "object": {
      "id": "test_payment_id",
      "status": "succeeded",
      "amount": {"value": "1000.00", "currency": "RUB"}
    }
  }'
```

**Важно:** В реальности формат webhook от YooMoney может отличаться. Проверьте документацию YooMoney.

---

## 🔒 Безопасность (важно для продакшена)

1. ✅ Секретные ключи в переменных окружения
2. ✅ Проверка подписи webhook
3. ⚠️ Добавьте аутентификацию для админ-эндпоинтов
4. ⚠️ Используйте HTTPS в продакшене
5. ⚠️ Ограничьте CORS только нужными доменами
6. ⚠️ Добавьте rate limiting для API

**Почему эти меры важны?** Защита от несанкционированного доступа и злоупотреблений.

---

## 📚 Документация YooMoney

- [Документация API](https://yoomoney.ru/docs/payment-buttons/using-api/payment-process)
- [Webhook документация](https://yoomoney.ru/docs/payment-buttons/using-api/webhooks)
- [Безакцептные списания](https://yoomoney.ru/docs/payment-buttons/using-api/recurring-payments)

---

## 🐛 Отладка

### Логи API

Все логи выводятся в консоль, где запущен `app.py`. В продакшене настройте логирование в файл.

### Проверка БД

Используйте SQLite Browser или командную строку:

```bash
sqlite3 shelter.db
.tables
SELECT * FROM donations LIMIT 10;
```

### Проверка статуса платежей

В админ-панели (`http://localhost:8000/frontend/admin.html`) на вкладке "Донаты" отображаются все донаты с их статусами.

---

## 📁 Файлы, связанные с пожертвованиями

- `frontend/donate.html` - страница пожертвований
- `frontend/js/donate.js` - логика обработки пожертвований и модального окна карты
- `frontend/js/donations-db.js` - локальная БД пожертвований
- `frontend/js/profile.js` - управление сохраненными картами в профиле
- `backend/app.py` - API для создания пожертвований
- `backend/database.py` - модели БД для пожертвований

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи API сервера
2. Проверьте консоль браузера (F12)
3. Проверьте статус платежей в YooMoney личном кабинете
4. Убедитесь, что webhook URL доступен извне

