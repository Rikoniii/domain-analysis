# API для приюта "Дом Лап"

## 📖 Что это?

API (Application Programming Interface) - это серверная часть проекта, которая обрабатывает запросы от веб-сайта. API отвечает за:
- Хранение пожертвований в базе данных
- Интеграцию с платежными системами (YooMoney)
- Управление подписками на регулярные пожертвования
- Предоставление данных для админ-панели

---

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

Перейдите в папку `backend/` и установите необходимые библиотеки:

```bash
cd backend
pip install -r requirements.txt
```

**Что это делает?** Устанавливает все необходимые Python-библиотеки для работы API (Flask, SQLAlchemy и др.)

### Шаг 2: Инициализация базы данных

База данных создастся автоматически при первом запуске API. Файл `shelter.db` появится в папке `backend/`.

**Если у вас уже есть данные в JSON файлах**, перенесите их в базу данных:

```bash
python migrate_data.py
```

### Шаг 3: Запуск API сервера

**Windows:**
```bash
start-api.bat
```

**Linux/Mac:**
```bash
cd backend
python app.py
```

API будет доступен на `http://localhost:5000`

**Что происходит?** Запускается веб-сервер, который принимает HTTP-запросы и обрабатывает их.

### Шаг 4: Запуск фронтенд-сервера (в отдельном терминале)

Откройте новый терминал и запустите:

```bash
start-server.bat
```

Фронтенд будет доступен на `http://localhost:8000/frontend/`

**Почему два сервера?** 
- API сервер (порт 5000) - обрабатывает данные и платежи
- Фронтенд сервер (порт 8000) - отдает HTML/CSS/JS файлы браузеру

### Шаг 5: Проверка работы

1. Откройте `http://localhost:8000/frontend/admin.html`
2. Перейдите на вкладку "Донаты"
3. Должны загрузиться данные из API (или показаться сообщение, что API недоступен)

**Если все работает:** Вы увидите список пожертвований в админ-панели.

---

## 📁 Структура файлов

```
backend/
├── app.py              # Основной файл API сервера (Flask)
├── database.py        # Модели базы данных и инициализация
├── migrate_data.py    # Скрипт для переноса данных из JSON в БД
├── server.py          # Локальный веб-сервер для разработки
├── requirements.txt   # Список зависимостей Python
└── shelter.db         # База данных SQLite (создается автоматически)
```

**Что делает каждый файл?**
- `app.py` - содержит все API эндпоинты и логику обработки запросов
- `database.py` - определяет структуру таблиц в базе данных
- `migrate_data.py` - переносит данные из JSON файлов в базу данных
- `requirements.txt` - список библиотек, которые нужно установить

---

## ⚙️ Настройка YooMoney (опционально)

Для работы с реальными платежами создайте файл `.env` в папке `backend/`:

```env
YOOMONEY_SHOP_ID=your_shop_id
YOOMONEY_SECRET_KEY=your_secret_key
YOOMONEY_WEBHOOK_SECRET=your_webhook_secret
BASE_URL=http://localhost:8000
```

**Важно:** 
- Без настроенных ключей API работает в **демо-режиме** (мок-платежи)
- В демо-режиме платежи не списываются реально, но система работает для тестирования
- Для получения ключей зарегистрируйтесь в [YooMoney для бизнеса](https://yoomoney.ru/business)

---

## 🔧 Решение проблем

### API не запускается

**Проблема:** При запуске появляется ошибка или сервер не стартует.

**Решение:**
1. Проверьте, что порт 5000 свободен (закройте другие программы, использующие этот порт)
2. Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
3. Проверьте, что вы находитесь в папке `backend/` при запуске

### Админка не загружает данные

**Проблема:** В админ-панели показывается сообщение "API недоступен" или данные не загружаются.

**Решение:**
1. Убедитесь, что API сервер запущен на порту 5000 (проверьте консоль, где запущен `app.py`)
2. Проверьте консоль браузера (F12) на наличие ошибок CORS (должны быть разрешены автоматически)
3. Проверьте, что в `frontend/js/admin.js` используется правильный URL API (`http://localhost:5000`)

### База данных не создается

**Проблема:** Файл `shelter.db` не появляется после запуска.

**Решение:**
1. Проверьте права на запись в директории `backend/`
2. Убедитесь, что SQLite установлен (обычно входит в Python)
3. Проверьте логи в консоли на наличие ошибок

---

## 📡 API Эндпоинты

### Админ-панель

#### `GET /api/admin/donations`
Получить список пожертвований для админ-панели.

**Параметры запроса (опционально):**
- `limit` - количество записей (по умолчанию 50)
- `status` - фильтр по статусу: `pending`, `succeeded`, `canceled`, `failed`

**Пример запроса:**
```
GET http://localhost:5000/api/admin/donations?limit=10&status=succeeded
```

**Пример ответа:**
```json
[
  {
    "id": 1,
    "public_name": "Анна Петрова Васильевна",
    "amount": 500.0,
    "purpose": "food",
    "status": "succeeded",
    "paid_at": "2024-10-15T12:34:56",
    "created_at": "2024-10-15T12:34:56",
    "phone": "+7 (495) 123-45-67",
    "email": "anna@example.com",
    "is_recurring": false
  }
]
```

#### `GET /api/admin/donations/monthly-stats`
Получить статистику пожертвований за месяц для графика.

**Параметры запроса (обязательно):**
- `year` - год (например, 2024)
- `month` - месяц (1-12)

**Пример запроса:**
```
GET http://localhost:5000/api/admin/donations/monthly-stats?year=2024&month=10
```

**Пример ответа:**
```json
{
  "year": 2024,
  "month": 10,
  "by_day": [
    {"day": 1, "amount": 2000},
    {"day": 2, "amount": 0},
    {"day": 3, "amount": 1500}
  ],
  "total": 45000
}
```

### Пожертвования

#### `POST /api/donations`
Создать новое пожертвование и инициировать платеж.

**Тело запроса:**
```json
{
  "amount": 1000,
  "purpose": "food",
  "is_recurring": false,
  "anonymous": false,
  "full_name": "Иванов Иван Иванович",
  "phone": "+7 900 000-00-00",
  "email": "test@example.com",
  "user_id": 123
}
```

**Поля:**
- `amount` - сумма пожертвования (число)
- `purpose` - назначение: `food`, `medical`, `maintenance`, `general`
- `is_recurring` - регулярное ли пожертвование (true/false)
- `anonymous` - анонимное ли пожертвование (true/false)
- `full_name` - полное имя донора
- `phone` - телефон
- `email` - email
- `user_id` - ID пользователя (опционально, если пользователь авторизован)

**Пример ответа:**
```json
{
  "donation_id": 456,
  "payment_url": "https://yoomoney.ru/checkout/...",
  "status": "pending"
}
```

### Webhook YooMoney

#### `POST /api/yoomoney/webhook`
Обработчик уведомлений от YooMoney о статусе платежа.

**Важно:** 
- Этот эндпоинт должен быть доступен извне для получения webhook от YooMoney
- В режиме разработки используйте [ngrok](https://ngrok.com/) для проброса портов
- В продакшене настройте HTTPS и разверните на сервере

**Как это работает:**
1. Пользователь совершает платеж на YooMoney
2. YooMoney отправляет уведомление на этот эндпоинт
3. API обновляет статус пожертвования в базе данных

### Подписки (регулярные пожертвования)

#### `GET /api/subscriptions`
Получить список подписок на регулярные пожертвования.

**Параметры запроса (опционально):**
- `user_id` - фильтр по пользователю
- `status` - фильтр по статусу: `active`, `canceled`, `paused`

#### `POST /api/subscriptions/<id>/cancel`
Отменить подписку на регулярное пожертвование.

**Пример запроса:**
```
POST http://localhost:5000/api/subscriptions/1/cancel
```

---

## 🗄️ Структура базы данных

База данных SQLite (`shelter.db`) содержит следующие таблицы:

- **users** - пользователи системы (телефон, email, имя)
- **payment_methods** - способы оплаты (токены от платежных провайдеров)
- **donations** - пожертвования (сумма, статус, дата и т.д.)
- **subscriptions** - регулярные пожертвования (подписки)

Подробнее о структуре БД см. файл `АНАЛИЗ-БАЗЫ-ДАННЫХ.md`

---

## ⏰ Крон-джоб для регулярных списаний

Для обработки регулярных списаний по подпискам нужно настроить задачу, которая будет периодически вызывать функцию `process_recurring_charges()`.

**Что это?** Функция проверяет все активные подписки и списывает средства с сохраненных карт пользователей.

**Пример для Windows Task Scheduler:**
```bash
python -c "from app import process_recurring_charges; process_recurring_charges()"
```

**Пример для Linux cron (каждый час):**
```
0 * * * * cd /path/to/project/backend && python -c "from app import process_recurring_charges; process_recurring_charges()"
```

---

## 🔄 Миграция данных

Для переноса существующих данных из JSON файлов в базу данных:

**Вариант 1: Через скрипт**
```bash
cd backend
python migrate_data.py
```

**Вариант 2: Через API**
```bash
curl -X POST http://localhost:5000/api/migrate/json-to-db
```

**Что переносится?** Данные из `data/donations.json` в таблицу `donations` в базе данных.

---

## 🐛 Отладка

API работает в режиме отладки (`debug=True`), что позволяет видеть подробные логи ошибок.

**Где смотреть логи?** В консоли, где запущен сервер (`app.py`).

**Что показывают логи?**
- Все HTTP-запросы к API
- Ошибки и исключения
- SQL-запросы к базе данных

---

## 🔒 Безопасность (важно для продакшена)

⚠️ **Перед развертыванием на продакшене обязательно:**

1. **Не храните секретные ключи в коде** - используйте переменные окружения или `.env` файл (не коммитьте его в Git!)

2. **Настройте HTTPS** - для webhook эндпоинта обязательно нужен HTTPS, иначе YooMoney не будет отправлять уведомления

3. **Реализуйте проверку подписи webhook** - согласно документации YooMoney, нужно проверять подпись каждого webhook-уведомления

4. **Используйте более надежную БД** - для продакшена лучше использовать PostgreSQL вместо SQLite

5. **Добавьте аутентификацию** - для админ-эндпоинтов (`/api/admin/*`) нужно добавить проверку прав доступа

6. **Настройте CORS правильно** - в продакшене разрешайте запросы только с вашего домена

---

## 📚 Следующие шаги

После успешного запуска API:

1. ✅ Настройте реальные ключи YooMoney для продакшена (см. `ПЛАТЕЖИ.md`)
2. ✅ Настройте крон-джоб для обработки регулярных списаний
3. ✅ Настройте HTTPS и развертывание на сервере для webhook
4. ✅ Изучите документацию по настройке SMS (см. `НАСТРОЙКА-SMS.md`)

---

**Нужна помощь?** Проверьте другие файлы документации:
- `ПЛАТЕЖИ.md` - как работают платежи и настройка для продакшена
- `НАСТРОЙКА-SMS.md` - настройка SMS-уведомлений
- `АНАЛИЗ-БАЗЫ-ДАННЫХ.md` - подробная информация о базе данных
