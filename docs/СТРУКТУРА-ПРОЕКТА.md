# Структура проекта

## 📖 Как мы это сделали и зачем?

Проект был реорганизован для лучшей читаемости и понимания структуры кода. Мы разделили проект на логические части: frontend, backend, docs, data.

**Почему именно такая структура?**
- **Разделение ответственности** - frontend и backend в разных папках, проще понимать и поддерживать
- **Удобство разработки** - каждый разработчик работает в своей области
- **Масштабируемость** - легко добавлять новые компоненты, не путаясь в структуре
- **Стандартность** - такая структура понятна любому разработчику

**Как это работает?** Frontend сервер отдает статические файлы (HTML, CSS, JS), а API сервер обрабатывает запросы и работает с базой данных. Они работают на разных портах и могут быть развернуты отдельно.

Проект реорганизован для лучшей читаемости и понимания структуры кода.

## Структура папок

```
proekt/
├── frontend/          # Frontend часть проекта
│   ├── *.html         # HTML страницы
│   ├── css/           # Стили
│   ├── js/            # JavaScript файлы
│   ├── images/        # Изображения
│   ├── manifest.json   # PWA манифест
│   └── sw.js          # Service Worker
│
├── backend/           # Backend часть проекта
│   ├── app.py         # Flask API сервер
│   ├── server.py      # Локальный веб-сервер для разработки
│   ├── database.py    # Модели базы данных
│   ├── migrate_data.py # Скрипт миграции данных
│   └── requirements.txt # Python зависимости
│
├── docs/              # Документация
│   ├── README.md
│   ├── API.md
│   ├── ПЛАТЕЖИ.md
│   └── ... (другие .md файлы)
│
├── data/              # Данные (JSON файлы)
│   ├── animals.json
│   ├── donations.json
│   └── ... (другие JSON файлы)
│
├── start-server.bat   # Запуск веб-сервера
└── start-api.bat      # Запуск API сервера
```

## Запуск проекта

### Веб-сервер (Frontend)
```bash
start-server.bat
```
Сервер запустится на `http://localhost:8000/frontend/`

### API сервер (Backend)
```bash
start-api.bat
```
API сервер запустится на `http://localhost:5000`

---

## Важные замечания

1. **Пути в коде**: Пути к `data/` в JS файлах используют `../data/`
2. **Service Worker**: Пути в `sw.js` настроены для работы с текущей структурой
3. **Батники**: Используются для запуска серверов

## Запуск проекта

Используйте батники для запуска:

- `start-server.bat` - запуск веб-сервера (откройте http://localhost:8000/frontend/)
- `start-api.bat` - запуск API сервера (http://localhost:5000)
